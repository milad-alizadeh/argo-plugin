---
---

<section class="anywhere">
  <h2 class="anywhere-heading">Works with any Claude Code project</h2>
  <p class="anywhere-lede">
    <code>/argo:init</code> detects your stack and, with per-rule consent, writes what a project actually keeps —
    nothing executable lands in your repo, that stays in the kit. Adapted, never imposed, never a fork.
  </p>
</section>

<style>
  .anywhere {
    max-width: 42rem;
    margin: 0 auto 4rem;
    padding: 0 1.5rem;
    text-align: center;
  }

  .anywhere-heading {
    font-size: clamp(1.4rem, 2.6vw, 1.85rem);
    margin: 0 0 0.75rem;
  }

  .anywhere-lede {
    color: var(--sl-color-gray-2, #b8bfd1);
  }

  .anywhere-lede code {
    color: var(--argo-color-accent, #5b7cfa);
  }
</style>
