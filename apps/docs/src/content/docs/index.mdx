---
title: Argo
description: Argo, an engineering way of working for Claude Code.
template: splash
---

import Hero from '../../components/Hero.astro'
import GateShowcase from '../../components/GateShowcase.astro'
import FigmaBidirectional from '../../components/FigmaBidirectional.astro'
import WorksAnywhere from '../../components/WorksAnywhere.astro'
import FinalCta from '../../components/FinalCta.astro'

<Hero
  headline="Claude Code, batteries included"
  subhead="A gated pipeline from PRD to shipped code, every stage owned by a skill or agent, every seam guarded by a mechanical gate, so quality is enforced, not requested."
  primaryCta={{ text: 'Get started', href: `${import.meta.env.BASE_URL.replace(/\/$/, '')}/start-here/what-is-argo/` }}
  secondaryCta={{ text: 'GitHub', href: 'https://github.com/milad-alizadeh/argo-plugin' }}
/>

<GateShowcase />

<FigmaBidirectional />

<WorksAnywhere />

<FinalCta
  docsHref={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/start-here/what-is-argo/`}
  githubHref="https://github.com/milad-alizadeh/argo-plugin"
/>
