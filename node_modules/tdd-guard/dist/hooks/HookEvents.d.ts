import { ToolOperation } from '../contracts/schemas/toolSchemas';
import { Storage } from '../storage/Storage';
export declare class HookEvents {
    private readonly storage;
    constructor(storage: Storage);
    processEvent(operation: ToolOperation | undefined): Promise<void>;
    private persistOperation;
}
//# sourceMappingURL=HookEvents.d.ts.map