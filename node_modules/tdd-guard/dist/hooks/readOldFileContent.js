"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.readOldFileContent = readOldFileContent;
const promises_1 = require("node:fs/promises");
const MAX_OLD_CONTENT_BYTES = 262_144;
async function readOldFileContent(filePath) {
    let handle;
    try {
        handle = await (0, promises_1.open)(filePath, 'r');
        const stats = await handle.stat();
        if (stats.size > MAX_OLD_CONTENT_BYTES)
            return '';
        return await handle.readFile('utf-8');
    }
    catch (error) {
        if (error instanceof Error &&
            error.code === 'ENOENT') {
            return '';
        }
        throw error;
    }
    finally {
        await handle?.close();
    }
}
