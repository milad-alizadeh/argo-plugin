import { GuardManager } from '../guard/GuardManager';
import { ValidationResult } from '../contracts/types/ValidationResult';
export declare class UserPromptHandler {
    private readonly guardManager;
    private readonly GUARD_COMMANDS;
    constructor(guardManager?: GuardManager);
    processUserCommand(hookData: string): Promise<ValidationResult | undefined>;
    getDisabledResult(): Promise<ValidationResult | undefined>;
}
//# sourceMappingURL=userPromptHandler.d.ts.map