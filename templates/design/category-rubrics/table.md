---
category: table
visualCriteria:
  - id: header-emphasis
    prompt: "Does the header row read as visually distinct from the body rows (weight, color, or a divider)?"
    requiresZoomedCrop: false
  - id: row-separation
    prompt: "Are adjacent rows visually separable at a glance (a divider, zebra striping, or spacing) rather than reading as one block?"
    requiresZoomedCrop: false
---

# table — category visual criteria

Human-authored once for this category (fidelity-geometry-verifier.md Slice
10). `assembleFidelityRubric` merges these fixed criteria with any brief-
named requirements; never edit this file to encode a single screen's
requirements — those belong in the screen brief, not here.
