---
category: tree
visualCriteria:
  - id: icon-identity
    prompt: "Does each node's leading icon match its semantic meaning (folder vs. leaf, type-specific glyphs)?"
    requiresZoomedCrop: true
  - id: expand-collapse-affordance
    prompt: "Does an expanded node's disclosure control read as visually distinct from a collapsed one?"
    requiresZoomedCrop: true
---

# tree — category visual criteria

Human-authored once for this category (fidelity-geometry-verifier.md Slice
10). `assembleFidelityRubric` merges these fixed criteria with any brief-
named requirements; never edit this file to encode a single screen's
requirements — those belong in the screen brief, not here.
