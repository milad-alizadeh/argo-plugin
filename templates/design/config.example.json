{
  "recipe": "{{RECIPE_NAME}}",
  "semanticCollectionName": "{{SEMANTIC_COLLECTION_NAME}}",
  "tokenFilePath": "{{TOKEN_FILE_PATH}}",
  "componentsPath": "{{COMPONENTS_PATH}}",
  "knownGoodTriad": {
    "storybook": "{{VERSION}}",
    "vitestAddon": "{{VERSION}}",
    "vitest": "{{VERSION}}"
  },
  "vrtEnvironment": {
    "browser": "{{PINNED_CHROMIUM_BUILD}}",
    "viewport": "{{W}}x{{H}}",
    "dpr": 1
  },
  "figma": {
    "projectFileKey": "{{PROJECT_FILE_KEY}}"
  },
  "design": {
    "componentCategories": ["primitive", "composite"]
  },
  "recipeConfig": {
    "figma": {
      "kitLibraryFileKey": "{{KIT_FILE_KEY}}"
    }
  },
  "_meta": {
    "setupVersion": "{{SETUP_VERSION}}",
    "managedFiles": []
  }
}
