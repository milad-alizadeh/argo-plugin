# argo graphify — committed: graph.json (embedded labels), GRAPH_REPORT.md,
# .graphify_labels.json. Ignored: bulk viz + caches (regenerable, not read by agents).
# setup-claude appends these to the project's .gitignore. Globs cover monorepo
# (per-workspace graphify-out/) and single-app (root graphify-out/) alike.
**/graphify-out/graph.html
**/graphify-out/cache/
**/graphify-out/.graphify_analysis.json
